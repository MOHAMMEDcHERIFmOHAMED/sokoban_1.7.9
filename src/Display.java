public class Display {
    public void render(Board board) {
        // This class is now optional as the GUI handles rendering.
        // You can remove it or keep it for console debugging.
    }
}
